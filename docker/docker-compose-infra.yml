services:

  # ============================================================
  # Databases & Middleware
  # ============================================================

  redis:
    container_name: ti-redis
    image: redis:7-alpine
    command: redis-server
    volumes:
      - redis-data:/data
    ports:
      - "6379:6379"
    network_mode: host

  rabbitmq:
    container_name: ti-rabbitmq
    image: rabbitmq:4-management
    hostname: rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-admin}
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
      - ./rabbitmq/definitions.json:/etc/rabbitmq/definitions.json
      - ./rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf
    ports:
      - "15672:15672"
      - "5672:5672"
    network_mode: host

  ti-knowledge-db:
    image: postgres:17
    container_name: ti-knowledge-db
    environment:
      POSTGRES_DB: ${KNOWLEDGE_DB_NAME:-knowledge_db}
      POSTGRES_USER: ${KNOWLEDGE_USER:-knowledge_user}
      POSTGRES_PASSWORD: ${KNOWLEDGE_PASSWORD:-qwerty}
    ports:
      - "5432:5432"
    volumes:
      - ./knowledge-init-scripts:/docker-entrypoint-initdb.d
      - knowledge-db-data:/var/lib/postgresql/data
    network_mode: host

  ti-document-db:
    image: ankane/pgvector
    container_name: ti-document-db
    environment:
      POSTGRES_DB: ${DOCUMENT_DB_NAME:-document_db}
      POSTGRES_USER: ${DOCUMENT_USER:-postgres}
      POSTGRES_PASSWORD: ${DOCUMENT_PASSWORD:-postgres}
    ports:
      - "5433:5432"
    volumes:
      - document-db-data:/var/lib/postgresql/data
      - ./document-init-scripts:/docker-entrypoint-initdb.d
    network_mode: host

  ti-assistant-db:
    image: postgres:17
    container_name: ti-assistant-db
    environment:
      POSTGRES_DB: ${ASSISTANT_DB_NAME:-assistant_db}
      POSTGRES_USER: ${ASSISTANT_USER:-assistant_user}
      POSTGRES_PASSWORD: ${ASSISTANT_PASSWORD:-qwerty}
    ports:
      - "5434:5432"
    volumes:
      # initialization scripts
      - ./assistant-init-scripts:/docker-entrypoint-initdb.d
      # PostgreSQL data
      - assistant-db-data:/var/lib/postgresql/data
    network_mode: host


  # ============================================================
  # Observability
  # ============================================================

  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: ti-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./observability/prometheus/prometheus-local.yml:/etc/prometheus/prometheus.yml:ro
    network_mode: host

  loki:
    image: grafana/loki:2.9.5
    container_name: ti-loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    command:
      - -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ./observability/loki/loki-config.yaml:/etc/loki/loki-config.yaml:ro
      - loki-data:/tmp/loki
    network_mode: host

  # ------------------------------------------------------------
  # Grafana Alloy - Docker logs -> Loki
  # ------------------------------------------------------------

  alloy:
    image: grafana/alloy:latest
    container_name: ti-grafana-alloy
    restart: unless-stopped

    command:
      - run
      - /etc/alloy/config.alloy

    volumes:
      - ./observability/alloy/config.alloy:/etc/alloy/config.alloy:ro

      # Docker container logs
      - /var/lib/docker/containers:/var/lib/docker/containers:ro

      # Docker metadata
      - /var/run/docker.sock:/var/run/docker.sock:ro

    network_mode: host

  zipkin:
    image: openzipkin/zipkin:3
    container_name: ti-zipkin
    ports:
      - "9411:9411"
    restart: unless-stopped
    network_mode: host

  grafana:
    image: grafana/grafana:10.4.0
    container_name: ti-grafana
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASS:-admin}
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning
      - grafana-data:/var/lib/grafana
    network_mode: host


# ==============================================================
# Volumes
# ==============================================================

volumes:
  knowledge-db-data:
  document-db-data:
  assistant-db-data:
  redis-data:
  rabbitmq-data:
  loki-data:
  grafana-data:

